{
  "name": "project-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "json-server": "json-server --watch backend/db.json"
  },
  "dependencies": {},
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
